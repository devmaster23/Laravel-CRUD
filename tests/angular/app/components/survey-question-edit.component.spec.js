ngDescribe({
    name: 'Test survey-question-edit component',
    modules: 'app',
    element: '<survey-question-edit></survey-question-edit>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
