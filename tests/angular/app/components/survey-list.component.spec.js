ngDescribe({
    name: 'Test survey-list component',
    modules: 'app',
    element: '<survey-list></survey-list>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
