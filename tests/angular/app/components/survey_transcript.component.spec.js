ngDescribe({
    name: 'Test survey-transcript component',
    modules: 'app',
    element: '<survey-transcript></survey-transcript>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
