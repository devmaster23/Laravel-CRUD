ngDescribe({
    name: 'Test survey-question-add component',
    modules: 'app',
    element: '<survey-question-add></survey-question-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
