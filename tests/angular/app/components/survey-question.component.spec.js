ngDescribe({
    name: 'Test survey-question component',
    modules: 'app',
    element: '<survey-question></survey-question>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
