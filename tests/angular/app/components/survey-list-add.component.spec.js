ngDescribe({
    name: 'Test survey-list-add component',
    modules: 'app',
    element: '<survey-list-add></survey-list-add>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
