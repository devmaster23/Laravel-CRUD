ngDescribe({
    name: 'Test survey-view component',
    modules: 'app',
    element: '<survey-view></survey-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
